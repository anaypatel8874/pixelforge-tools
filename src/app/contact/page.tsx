export const metadata = { title: 'Contact' };

export default function ContactPage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-12 prose dark:prose-invert">
      <h1>Contact</h1>
      <p>
        Have a feature request, found a bug, or want to suggest a tool?
        Email us at <code>hello@pixelforge.tools</code> (placeholder — replace
        with your real address in the footer component).
      </p>
    </div>
  );
}
